<Project>
	<PropertyGroup
		Condition="'$(Configuration)' == 'Release' And '$(BuildPackage)' == 'true' And '$(env_build_package)' == 'true'">
		<GeneratePackageOnBuild>true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>